# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库定位

本仓库是**股票投资分析文档库**，不是代码项目——没有构建、测试或运行命令。所有文档使用中文。

## 核心工作流：投资分析

1. **分析框架**：`框架_v5.md` 是当前标准。V1-V4 是历史版本，新分析一律使用 V5 框架。
2. **分析流程**：用户请求"分析某只股票"时，优先使用 `stock-analyzer` 技能（项目根目录 `SKILL.md`）自动执行：数据采集（WebFetch/WebSearch，行情数据源为 `quote.eastmoney.com` 和 `emweb.securities.eastmoney.com`）→ 按 V5 框架 A/B/C 三层填充 → 20 项质量检查。**全流程一次性连续执行到底，不请求中间确认**（用户明确要求分阶段或数据源全部失败时除外）。
3. **证据分级**：所有数据必须标注证据等级——A（年报/公告）、B（交易所行情）、C（署名研报/权威媒体）、D（推断，须说明方法）。仅有 C/D 级证据时，结论置信度不得高于"中"。
4. **必备要素**（V5 框架中反复出现的硬性要求）：数据字典（行情日期/币种/财报期）、安全边际与概率加权价值分开计算、行为偏误快检、风险登记表（每项风险需 🟡/🟠/🔴 三级触发阈值）、"如果今天空仓还愿意买吗"测试。
5. **免责声明**：每份分析报告开头必须包含"本报告用于研究与决策辅助，不构成证券投资建议"。

## 文件命名规范

- 框架：`框架_v{n}.md`
- 分析报告：`{公司名}_{股票代码}_投资分析.md`，同一公司的更新版本加后缀：`{公司名}_{代码}_投资分析_V{n}.md`
- 版本演进示例（长盈精密）：`投资分析.md` → `投资分析_V4.md` → `投资分析_V4.2.md` → `投资分析_V5.md`

## 数据来源约定

- 行情页：`https://quote.eastmoney.com/sz{code}.html`（深市）或 `sh{code}`（沪市）
- 财务数据页：`emweb.securities.eastmoney.com`
- 分析报告中行情数据需注明"截至时间/来源/证据等级"，并提示以交易所实时行情复核

## 行情数据管道

- `python fetch_market_data.py {代码}`：三源抓取（东财 ulist / 腾讯 / 新浪），当日缓存于 `data/market/{代码}_{日期}.json`；同一天重复运行直接读缓存不发请求；`--force` 强制重抓
- `python validate_market_data.py {代码} data/market/{代码}_{日期}.json`：校验价格、时间戳、代码及多源价格一致性
- 已知问题：东财 `push2` 实时接口存在**间歇性连接重置**（路径级反爬），主数据回退链自动兜底（东财 → 新浪 → 腾讯）；回退后估值与市值字段缺失，校验器会给出 warning

## Git 约定

- 提交信息用中文，前缀惯例：`新增：`、`修订：`、`初始化：`
- 只在用户明确要求时提交（commit）
- 当前分支为 master

## 安全边界

高风险/不可逆操作执行前必须通知用户并取得确认：删除文件或目录（含旧数据文件、旧版报告）、覆盖已有历史报告、git 危险操作（force push、reset --hard、删除分支、改写历史）、对仓库外文件的修改、首次运行未验证的新脚本。例行数据管道写入与新建分析报告可自主执行。
